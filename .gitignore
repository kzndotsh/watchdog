# Secrets
.env
.env.*
/tmp
*.pem
*.key

# Tooling
# .cursor/ is an allowlist, not a blanket ignore — see .cursor/README.md.
# Tracked: hooks/ (portable), hooks.json, README.md.
# Local-only: rules/, skills/, agents/ (third-party tool installs), plans/, settings.json.
.cursor/*
!.cursor/hooks
!.cursor/hooks/**
!.cursor/hooks.json
!.cursor/README.md
_legacy-v2/
.desloppify/
node_modules/
.venv/
dist/
.turbo/
*.tsbuildinfo
.cache/
*.log
pnpm-debug.log*
coverage/
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Regenerable platform output
export/
.wd-runtime/
scorecard.png

# Web build
apps/web/dist/
apps/web/.output/
apps/web/dist-ssr/
apps/web/.nitro/
apps/web/.wrangler/
apps/web/.vinxi/
apps/web/.tanstack/
apps/web/todos.json

# evlog / audit drains
apps/web/.evlog/
apps/worker/.evlog/
apps/web/.audit/

# Nix
.direnv/

# OS / IDE
.DS_Store
Thumbs.db
.idea/

# Local
CLAUDE.local.md
*.local
lefthook-local.yml

# Vendored Effect source for agents (nested clone; never commit)
repos/

# Scratch / batch assessment output
results/
