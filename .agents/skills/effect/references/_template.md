# Reference template

Load this when: adding a new reference file under this skill.

Copy this file, rename it, replace the body, and keep an explicit
**Load this when:** line at the top so `pnpm validate:agents` stays happy.
